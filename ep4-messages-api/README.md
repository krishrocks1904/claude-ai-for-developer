# Episode 4: The Messages API

## What you'll learn
- system / user / assistant role structure
- Stateless by default — managing conversation history manually
- Key parameters: max_tokens, temperature, stop_sequences
- Reading and parsing the response object

## Run the examples
```bash
python basic_request.py          # Single call, every response field annotated
python conversation_history.py   # Multi-turn without a framework
python parameters_demo.py        # temperature and stop_sequences in action
```
